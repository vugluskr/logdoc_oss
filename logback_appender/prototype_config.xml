<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true">
    <appender class="ru.gang.logdoc.appenders.LogdocTcpAppender" name="LOGDOC">
        <!-- Mandatory fields -->
        <host>127.0.0.1</host>
        <port>7686</port>

        <!-- Optional fields -->
        <prefix/> <!-- absolute prefix for any logger name (source) -->
        <suffix/> <!-- absolute suffix for any logger name (source) -->
    </appender>

    <root level="DEBUG">
        <appender-ref ref="LOGDOC"/>
    </root>
</configuration>
